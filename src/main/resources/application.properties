spring.application.name=nexusai-gateway
spring.config.import=optional:file:.env[.properties]
groq.api.key=${GROQ_API_KEY:}
groq.api.url=https://api.groq.com/openai/v1/chat/completions

gemini.api.key=${GEMINI_API_KEY:}
# NOTE: The model is NO LONGER embedded in the URL. The GeminiProvider reads the model dynamically
# from ModelRegistry. The URL below is the base template without a model suffix.
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models

openai.api.key=${OPENAI_API_KEY:}
openai.api.url=https://api.openai.com/v1/chat/completions

claude.api.key=${CLAUDE_API_KEY:}
claude.api.url=https://api.anthropic.com/v1/messages

ollama.api.url=http://localhost:11434/v1/chat/completions

# ============================================================
# MODEL CATALOGUE — Single source of truth for all model arms.
# To add a new provider/model, add a new block here.
# No Java code changes are required.
# Format: nexusai.models.<logical-name>.<property>=<value>
# ============================================================

# --- Groq Models ---
nexusai.models.groq-70b.provider=groq
nexusai.models.groq-70b.model-id=qwen/qwen3.6-27b
nexusai.models.groq-70b.input-price-per1m=0.59
nexusai.models.groq-70b.output-price-per1m=0.79
nexusai.models.groq-70b.estimated-latency-ms=350
nexusai.models.groq-70b.context-window-tokens=128000
nexusai.models.groq-70b.enabled=true

nexusai.models.groq-8b.provider=groq
nexusai.models.groq-8b.model-id=allam-2-7b
nexusai.models.groq-8b.input-price-per1m=0.05
nexusai.models.groq-8b.output-price-per1m=0.08
nexusai.models.groq-8b.estimated-latency-ms=120
nexusai.models.groq-8b.context-window-tokens=131072
nexusai.models.groq-8b.enabled=true

# --- Gemini Models ---
nexusai.models.gemini-2-5-flash.provider=gemini
nexusai.models.gemini-2-5-flash.model-id=gemini-2.5-flash
nexusai.models.gemini-2-5-flash.input-price-per1m=0.075
nexusai.models.gemini-2-5-flash.output-price-per1m=0.30
nexusai.models.gemini-2-5-flash.estimated-latency-ms=250
nexusai.models.gemini-2-5-flash.context-window-tokens=1048576
nexusai.models.gemini-2-5-flash.enabled=true

nexusai.models.gemini-3-1-pro.provider=gemini
nexusai.models.gemini-3-1-pro.model-id=gemini-3.1-pro-preview
nexusai.models.gemini-3-1-pro.input-price-per1m=1.25
nexusai.models.gemini-3-1-pro.output-price-per1m=5.00
nexusai.models.gemini-3-1-pro.estimated-latency-ms=800
nexusai.models.gemini-3-1-pro.context-window-tokens=1048576
nexusai.models.gemini-3-1-pro.enabled=false

# --- OpenAI Models (disabled until key is set) ---
nexusai.models.gpt-4o-mini.provider=openai
nexusai.models.gpt-4o-mini.model-id=gpt-4o-mini
nexusai.models.gpt-4o-mini.input-price-per1m=0.15
nexusai.models.gpt-4o-mini.output-price-per1m=0.60
nexusai.models.gpt-4o-mini.estimated-latency-ms=900
nexusai.models.gpt-4o-mini.context-window-tokens=128000
nexusai.models.gpt-4o-mini.enabled=false

nexusai.models.gpt-4o.provider=openai
nexusai.models.gpt-4o.model-id=gpt-4o
nexusai.models.gpt-4o.input-price-per1m=2.50
nexusai.models.gpt-4o.output-price-per1m=10.00
nexusai.models.gpt-4o.estimated-latency-ms=1200
nexusai.models.gpt-4o.context-window-tokens=128000
nexusai.models.gpt-4o.enabled=false

# --- Anthropic Claude Models (disabled until key is set) ---
nexusai.models.claude-haiku.provider=claude
nexusai.models.claude-haiku.model-id=claude-3-5-haiku-latest
nexusai.models.claude-haiku.input-price-per1m=0.80
nexusai.models.claude-haiku.output-price-per1m=4.00
nexusai.models.claude-haiku.estimated-latency-ms=700
nexusai.models.claude-haiku.context-window-tokens=200000
nexusai.models.claude-haiku.enabled=false

nexusai.models.claude-sonnet.provider=claude
nexusai.models.claude-sonnet.model-id=claude-sonnet-4-5
nexusai.models.claude-sonnet.input-price-per1m=3.00
nexusai.models.claude-sonnet.output-price-per1m=15.00
nexusai.models.claude-sonnet.estimated-latency-ms=1100
nexusai.models.claude-sonnet.context-window-tokens=200000
nexusai.models.claude-sonnet.enabled=false

# --- Ollama Local Models (free, local) ---
nexusai.models.ollama-llama3.provider=ollama
nexusai.models.ollama-llama3.model-id=llama3
nexusai.models.ollama-llama3.input-price-per1m=0.0
nexusai.models.ollama-llama3.output-price-per1m=0.0
nexusai.models.ollama-llama3.estimated-latency-ms=2000
nexusai.models.ollama-llama3.context-window-tokens=8192
nexusai.models.ollama-llama3.enabled=false


# Resilience4j Circuit Breaker configuration for Gemini
resilience4j.circuitbreaker.instances.gemini.failure-rate-threshold=50
resilience4j.circuitbreaker.instances.gemini.minimum-number-of-calls=3
resilience4j.circuitbreaker.instances.gemini.sliding-window-type=count_based
resilience4j.circuitbreaker.instances.gemini.sliding-window-size=5
resilience4j.circuitbreaker.instances.gemini.wait-duration-in-open-state=15s
resilience4j.circuitbreaker.instances.gemini.permitted-number-of-calls-in-half-open-state=2
resilience4j.circuitbreaker.instances.gemini.automatic-transition-from-open-to-half-open-enabled=true

# Resilience4j Circuit Breaker configuration for Groq
resilience4j.circuitbreaker.instances.groq.failure-rate-threshold=50
resilience4j.circuitbreaker.instances.groq.minimum-number-of-calls=3
resilience4j.circuitbreaker.instances.groq.sliding-window-type=count_based
resilience4j.circuitbreaker.instances.groq.sliding-window-size=5
resilience4j.circuitbreaker.instances.groq.wait-duration-in-open-state=15s
resilience4j.circuitbreaker.instances.groq.permitted-number-of-calls-in-half-open-state=2
resilience4j.circuitbreaker.instances.groq.automatic-transition-from-open-to-half-open-enabled=true

# Database Configuration
# spring.datasource.url=jdbc:h2:mem:nexusdb;DB_CLOSE_DELAY=-1
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Enable H2 console for debugging
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console
# spring.h2.console.settings.web-allow-others=true

# JPA/Hibernate properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# PostgreSQL Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/nexusai}
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgrespassword}

# Server Port (Required for Render)
server.port=${PORT:8080}

# JWT Security
jwt.secret=${JWT_SECRET:nexusai_super_secret_key_production_grade_32_chars}

# OpenAI API configuration
openai.api.key=${OPENAI_API_KEY:}
openai.api.url=https://api.openai.com/v1/chat/completions
openai.model=gpt-4o-mini

# Anthropic Claude API configuration
claude.api.key=${CLAUDE_API_KEY:}
claude.api.url=https://api.anthropic.com/v1/messages
claude.model=claude-3-5-haiku-latest

# Ollama local configuration
ollama.api.url=http://localhost:11434/v1/chat/completions
ollama.model=llama3

# Toggle to automatically mock providers if their API keys are not set/paid
gateway.mock-missing-providers=false

# Redis Connection Configurations
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}

# Rate Limiter Configuration Settings (Token Bucket only in production path)
# Sliding Window and Leaky Bucket are available in RateLimitExperimentService for benchmarking
gateway.rate-limiter.user.limit=5
gateway.rate-limiter.tenant.limit=20
gateway.rate-limiter.provider.gemini.limit=15
gateway.rate-limiter.provider.groq.limit=30
gateway.rate-limiter.provider.openai.limit=20
gateway.rate-limiter.provider.claude.limit=20
gateway.rate-limiter.provider.ollama.limit=100

# Spring Boot Actuator & Prometheus Observability Configuration
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.prometheus.enabled=true
management.metrics.export.prometheus.enabled=true

# ============================================================
# AEDF — Adaptive Enterprise Decision Framework Configuration
# ============================================================

# Active routing strategy: STATIC | RULE_BASED | WEIGHTED | ADAPTIVE
# Change this single property to switch between experimental baselines
nexusai.routing.strategy=FEDERATED

# Static routing defaults (used when strategy=STATIC)
# (Keys completely removed to ensure zero hardcoded routing overrides)

# LinUCB exploration parameter (used when strategy=ADAPTIVE)
# Higher alpha = more exploration, lower alpha = more exploitation
nexusai.routing.linucb.alpha=1.0

# Enterprise Policy Filter
# IMPORTANT: Do NOT hardcode providers here. Approved providers are stored per-tenant
# in the database (tenant_configs table) and controlled via the dashboard.
# This global list is intentionally left empty.
nexusai.policy.approved-providers=
nexusai.policy.blocked-providers=

# ============================================================
# AEDF Reward Weights — Policy Tier Configuration (Q5 Fix)
# ============================================================
# Active policy tier: ENTERPRISE | BUDGET | MISSION_CRITICAL
# The RewardCalculator reads the weights for the active tier.
# Changing this single property reshapes what the bandit optimizes for.
nexusai.reward.active-tier=ENTERPRISE

# ENTERPRISE tier — optimize quality first, then reliability
# Use case: internal enterprise tools, knowledge-base Q&A
nexusai.reward.tier.enterprise.quality=0.60
nexusai.reward.tier.enterprise.availability=0.20
nexusai.reward.tier.enterprise.latency=0.10
nexusai.reward.tier.enterprise.cost=0.10

# BUDGET tier — optimize cost first, then latency
# Use case: high-volume, non-critical classification tasks
nexusai.reward.tier.budget.quality=0.15
nexusai.reward.tier.budget.availability=0.05
nexusai.reward.tier.budget.latency=0.30
nexusai.reward.tier.budget.cost=0.50

# MISSION_CRITICAL tier — optimize quality and reliability at any cost
# Use case: medical/finance/legal domain requests
nexusai.reward.tier.mission-critical.quality=0.70
nexusai.reward.tier.mission-critical.availability=0.20
nexusai.reward.tier.mission-critical.latency=0.05
nexusai.reward.tier.mission-critical.cost=0.05

# ============================================================
# EMAIL / SMTP CONFIGURATION
# Set MAIL_HOST, MAIL_PORT, MAIL_USER, MAIL_PASS in .env or
# the Render environment panel. Mail is optional — if not set,
# the system will log notifications instead of emailing them.
# ============================================================
spring.mail.host=${MAIL_HOST:}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USER:}
spring.mail.password=${MAIL_PASS:}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_STARTTLS:true}
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
nexusai.mail.from=${MAIL_FROM:noreply@nexusai.dev}
nexusai.app.url=${APP_URL:http://localhost:5173}




